{
  "name": "@leader-sync/db",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "seed": "tsx seed/index.ts",
    "seed:perf": "tsx seed/perf-templates.ts",
    "studio": "drizzle-kit studio",
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0",
    "postgres": "^3.4.0",
    "@leader-sync/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.0",
    "tsx": "^4.0.0",
    "dotenv": "^16.0.0",
    "typescript": "^5.9.0"
  }
}
